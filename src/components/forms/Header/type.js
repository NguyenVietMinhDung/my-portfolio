import type { Props as SocialNetworks } from '../../commons/SocialNetworks/type';

export type Props = {
  socialNetworks: SocialNetworks,
  name: string,
  description: string,
  avatarUrl: string,
  btnTxt: string,
};
